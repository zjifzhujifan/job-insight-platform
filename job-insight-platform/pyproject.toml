[project]
name = "job-insight-platform"
version = "1.0.0"
description = "招聘数据爬取、分析与可视化平台"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-v --tb=short"

[tool.pytest-cov]
source = ["app"]
